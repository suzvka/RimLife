- 在 `PawnPro` 及相关 Info 类中广泛使用空值合并赋值运算符（??=）实现属性的延迟初始化与缓存。
- MCP 工具提供者统一实现 `IMcpHookProvider` 接口，并通过 `[McpTool]` 特性声明工具元数据与参数描述。
- 核心单例或服务组件（如 `LlmAccessor`、`InteractionStore`）采用双重检查锁定（Double-Checked Locking）模式确保线程安全的延迟加载。
- UI 页面实现统一的 `IConfigPage` 接口，通过 `Id`、`Group` 和 `Order` 属性由布局引擎自动组织侧栏导航与内容渲染。