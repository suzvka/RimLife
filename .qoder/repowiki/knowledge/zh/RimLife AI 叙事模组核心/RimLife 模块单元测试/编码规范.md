- 测试类命名采用“被测组件名 + Tests”后缀（如 `MemoryConsolidatorTests`），并附带 XML 摘要说明测试范围。
- 测试方法命名遵循“方法名_场景_预期结果”的三段式规范（如 `BuildRequest_EmptyList_ReturnsNull`）。
- 广泛使用 xUnit 的 Assert 断言库（如 `Assert.NotNull`, `Assert.Single`, `Assert.Contains`）进行精确的状态校验。